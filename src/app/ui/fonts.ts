import { NextFont } from 'next/dist/compiled/@next/font';
import { Inter, Montserrat } from 'next/font/google';

export const inter: NextFont = Inter({ subsets: ["latin"] });
export const montserrat: NextFont = Montserrat({ subsets: ['latin']});